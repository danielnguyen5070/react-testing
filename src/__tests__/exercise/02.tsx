// import * as React from 'react'
// import { render, screen } from '@testing-library/react'
// import userEvent from '@testing-library/user-event'
// import Login from '../../components/login'

test('submitting the form calls onSubmit with username and password', async () => {
    // 🐨 declare a variable to store submitted data
    // 🐨 define a handleSubmit function that assigns submittedData to the received form data

    // 🐨 render the Login component with handleSubmit passed as the onSubmit prop

    // 🐨 define the username and password to be used in the test

    // 🐨 type the username into the username input field
    // 🐨 type the password into the password input field

    // 🐨 click the submit button

    // 🧪 assert that submittedData equals the expected username and password object
})
